ROOT (conditionally renders):
  > ONBOARDING
    > addNewLanguage
    > onboardingSlides
  > loading
  > MAINDRAWER
    > drawer
    > MAINSTACK
      > lessonlist
      > play 
      > groups
      > addEditGroup (add)
      > addEditGroup (edit)
      > addNewLanguage
      > storage 
      > mt
      > passcode
      > security
      > securityOnboarding  
      > keyOrderSet (5 variants) 
      > game
      > SETSROOT
        > SETTABS
          > set (core)
          > set (topical)
          > set (mt) (rendered conditionally)
        > ADDSETSTACK
          > addSet
          > setInfo